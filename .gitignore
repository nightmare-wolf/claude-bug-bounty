# Claude Code local settings — contains session tokens and engagement-specific
# tool permissions. Never commit.
.claude/settings.local.json

# Sensitive output — never commit these
findings/
recon/
reports/
hunt-memory/
config.json
*.burp
*.json.bak

# API keys and tokens
.env
secrets.txt
api_keys.txt
.private/
# Filled-in OSINT key sheets — contain LIVE keys, never commit
*_FILLED.md
**/API_KEYS_FILLED.md

# Nested local clones / sub-repos (not tracked)
claude-bug-bounty/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# macOS
.DS_Store
.AppleDouble

# Logs
*.log
*.txt.bak

# Tool output
*.out
nmap-output/
